export const CartTrigger = (props: { count?: any }) => {
 
  return (
    <>
      {/* <button className="button" onClick={() => setOpenCart(true)}> */}
      <button className="button">Cart ()</button>
    </>
  );
};
